import User from "/user.class";

import class User{
    constructor(){
        let data = [];
    }

    populate(datos){
        data = datos;
    }

    addUser(datosNewUser){
        data.push(datosNewUser);
    }

    removeUser(userId){
        data.pop(user.id === userId);
    }

    changeUser(){

    }
}
import data from "../services/datos.js";
